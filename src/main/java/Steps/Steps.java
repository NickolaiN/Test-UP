package Steps;

public interface Steps {
    steps.DescSteps DESC_STEPS = new steps.DescSteps();
    PassSteps PASS_STEPS = new PassSteps();
    ApiSteps API_STEPS = new ApiSteps();
    NumGenSteps NUM_GEN_STEPS = new NumGenSteps();

}
